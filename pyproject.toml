[project]
name = "anonymous-discord"
version = "0.1.0"
description = "Discord App to run the Anonymous Whisper Mode in discord"
authors = [
    {name = "DrProfSgtMrJ",email = "bacon388@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "discord-py (>=2.7.1,<3.0.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "motor (>=3.3.0,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
